name: broadcatreceiver
description: "A new Flutter project."
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0" # Update as per your Flutter SDK version

dependencies:
  flutter:
    sdk: flutter

  connectivity_plus: ^2.0.2 # For Internet connectivity monitoring
  battery_plus: ^2.0.2 # For battery level monitoring
  shared_preferences: ^2.0.7 # For storing light/dark mode preference
  firebase_core: ^2.15.0 # Firebase core functionality
  firebase_auth: ^4.7.0 # Firebase authentication services
  google_sign_in: ^5.3.0 # Google sign-in
  flutter_facebook_auth: ^5.0.3 # Facebook sign-in
  provider: ^6.0.3 # State management for ThemeProvider
  logger: ^1.0.0 # Logging framework

  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0 # Recommended linting rules for Flutter

flutter:
  uses-material-design: true

  # Define your assets and fonts if needed
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  #
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
